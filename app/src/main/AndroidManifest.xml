<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kukugtu.autolayoutdemo">

    <application
        android:name=".AutoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="design_width"
            android:value="1080"/>
        <meta-data
            android:name="design_height"
            android:value="1920"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="uiMode|screenLayout|orientation|screenSize|smallestScreenSize|mcc|mnc"
            android:theme="@style/SwipeBack.Transparent.Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ExtraActivity"
            android:configChanges="uiMode|screenLayout|orientation|screenSize|smallestScreenSize|mcc|mnc">
        </activity>
    </application>

</manifest>